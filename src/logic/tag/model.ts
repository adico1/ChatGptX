/**
 * @fileoverview Defines the Tag interface used in the ChatGptX Chrome Extension.
 * @author Adico
 * @license MIT
 */

/**
 * Represents a tag for a content item in the extension.
 *
 * @interface
 */
export default interface Tag {
  id: string
  name: string
}